# local/

Scripts the agent wrote itself to get past a wall, using only the tools and connections it
already has (see CLAUDE.md, "When you hit a wall"). Each one is logged with
`node tools/requests.mjs add built ...` so Jaiah sees it and can move it into tools/ once it
has earned its place. The first run of any script here asks Tariq on Telegram.
