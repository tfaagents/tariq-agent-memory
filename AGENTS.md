# Agents

This folder is the agent. The model underneath is a power source and can be swapped.
Whatever runtime reads this file: the identity, rules and layout are in `CLAUDE.md`.
Memory is in `memory/`, source material in `raw/`, the record of work in `sessions/`,
repeatable jobs in `.claude/skills/`, the only bridges to TFA's systems in `tools/`.
