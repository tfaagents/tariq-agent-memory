{
  "_note": "Unattended runs, Brisbane time, run by tools/scheduler.mjs (started by start.sh). days = daily | weekdays | a comma list of mon..sun. The agent appends here through .claude/hooks/schedule.sh when Tariq says yes to a schedule; a slug must exist in .claude/skills/. Remove a line to stop it.",
  "jobs": [
    {
      "slug": "rollover",
      "time": "05:00",
      "days": "daily",
      "command": ".claude/hooks/rollover.sh"
    },
    {
      "slug": "inbox-watch",
      "every": 10,
      "days": "weekdays",
      "enabled": false,
      "_why_off": "needs the Tariq Assistant app in Entra and the tariq user; flip enabled to true then"
    },
    {
      "slug": "jaiah-digest",
      "time": "17:30",
      "days": "daily"
    }
  ],
  "_temp": "morning-send, nightly-learn and retro run from launchd LaunchAgents under tfaagents (loaded by Jaiah 10 Sep); only the rollover runs from this scheduler here so nothing runs twice. On the move to the tariq user the repo copy (all four) replaces this file and launchd is retired."
}
