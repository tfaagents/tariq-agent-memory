{
  "_note": "The browser lane. Chrome on this machine, its own profile (nothing of Tariq's is logged in unless added on purpose), remote debugging on a port of its own. READING IS OPEN: goto, text, links, forms, shot and js work on any http or https page, because opening a page and screenshotting it sends nothing anywhere. allowedHosts gates ACTING only: type, click, submit and upload, which touch someone else's system; submit and upload also ask Tariq on Telegram. Add a host when he wants a form filled, not when he wants a page read. httpbin.org and example.com are for tests. dashboardUrl is the TFA Agents dashboard as this machine reaches it; `node tools/tfa.mjs browser-login` puts Tariq's session into the profile so the worker can screenshot it (127.0.0.1 is on the list for that alone).",
  "dashboardUrl": "http://127.0.0.1:4680",
  "port": 9223,
  "headless": true,
  "profileDir": "work/browser/profile",
  "chrome": "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome",
  "allowedHosts": [
    "httpbin.org",
    "example.com",
    "127.0.0.1"
  ]
}
